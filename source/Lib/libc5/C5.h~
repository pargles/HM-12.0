#pragma once
#include "libc5.h"

//! \ingroup libMD5
//! \{

class MD5
{
public:

  /**
   * compute digest over buf of length len.
   * multiple calls may extend the digest over more data.
   */
  void hello()
  {
    printHelloWorld();
  }

}
//! \}
